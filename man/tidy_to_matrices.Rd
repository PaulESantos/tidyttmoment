% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_to_matrices.R
\name{tidy_to_matrices}
\alias{tidy_to_matrices}
\title{Convert tidy long data to community and trait matrices}
\usage{
tidy_to_matrices(df, comm_names, sp_names, trait_names, trait_value, weight)
}
\arguments{
\item{df}{A data frame containing the trait and community data.}

\item{comm_names}{Unquoted column name containing the community names/IDs.}

\item{sp_names}{Unquoted column name containing the species names/IDs.}

\item{trait_names}{Unquoted column name containing the trait IDs.}

\item{trait_value}{Unquoted column name containing the actual trait values.}

\item{weight}{Unquoted column name containing the variable used to weight (e.g., abundance).}
}
\value{
A list containing \code{site_species} (a site by species matrix) and \code{species_traits} (a species by trait matrix).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Helper function to pivot a tidy long-format data frame into a site-by-species abundance matrix
and a species-by-trait matrix, which are required for indices like functional diversity and rarity.
}
\examples{
df <- data.frame(
  comm = c("A", "A", "B", "B"),
  species = c("sp1", "sp2", "sp1", "sp3"),
  trait = c("height", "height", "height", "height"),
  trait_value = c(5, 10, 5, 15),
  abundance = c(1, 2, 1, 3)
)
tidy_to_matrices(df, comm, species, trait, trait_value, abundance)
}
